import { DefaultTheme } from 'styled-components'

const appTheme: DefaultTheme = {
    colors: {
        screenBackground: '#25272E',
        white: '#FFFFFF'
    },
    dimensions: {
        screenWidth: '71.87rem',
        screenPadding: '0.93rem',
    }
}

export default appTheme